# Dependencies
node_modules

# Build outputs
out
dist
release
*.tsbuildinfo

# OS / editor
.DS_Store
Thumbs.db
*.local
.vscode
.idea

# Environment / secrets
.env
.env.*
*.log

# Tooling-local state (not part of the project)
.claude
openspec

# NOTE: resources/talent-pool.enc IS committed (private xizim repo) so CI/mac
# builds — which lack the untracked sources/ — still ship the talent market.
# Re-commit it after updating the pool (pack-talent regenerates it on local builds).
