{
  "// 说明": "SuperStudio 客户端默认模型远程配置。客户端启动时从本仓库拉取此文件，作为各类生成的「推荐默认模型」。",
  "// 托管式默认": "仅当用户没有手动改过对应默认项时才生效；用户一旦在「设置 → 默认模型」里自选模型，本文件不再覆盖该项。",
  "// 字段说明": "只填模型「名称」，不填 providerId —— 客户端会按用户已登录的供应商自动解析由谁来跑这个模型。留空或删除某字段表示该类不下发推荐。",
  "// 注释": "本文件按 JSON 解析，但允许以 # 或 // 开头的整行注释，以及对象末尾多余逗号。",

  "defaultChatModel": "gpt-4o",
  "defaultImageModel": "dall-e-3",
  "defaultVideoModel": "doubao-seedance-2-0",
  "defaultEmbeddingModel": "text-embedding-3-small"
}
